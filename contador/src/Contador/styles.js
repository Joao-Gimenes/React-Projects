import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    area:{
        marginTop: 80
    },
    titulo:{
        fontSize: 30,
        color: 'black',
        textAlign: 'center'
    },
    contador:{
        marginTop: 15,
        fontSize: 60,
        color: '#black',
        textAlign: 'center',
        marginBottom: 10,
    },
});


export {styles}